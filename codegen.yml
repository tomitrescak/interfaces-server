overwrite: true
schema: 'http://localhost:4000'
documents: null
generates:
  src/generated/graphql.ts:
    config:
      enumsAsTypes: true
    plugins:
      - 'typescript-common'
      - 'typescript-server'
      - 'typescript-resolvers'
